eureka:
  client:
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8081/eureka
  instance:
    hostname: localhost
    
server:
  port: 8085
  
spring:
  application:
    name: user-service
